version: "3.9"

services:
  smart-home-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    restart: unless-stopped
